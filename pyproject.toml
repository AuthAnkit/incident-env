[project]
name = "incident-env"
version = "1.0.0"
description = "SRE Incident Response Simulator — OpenEnv compliant"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
inference = "inference:main"
